# Verify BookBub deals are still live on Amazon (NordVPN + fingerprint rotation)

Task: The next step in processing the database is to verify the deals are still live on Amazon
1. NordVPN details - Username - rivalon@gmail.com , Password - fgh0wnx6fvx1DUG!fud
2. Create a VPN tunnel
3. Using the VPN Tunnel, we will process  each book

4) After checking 10 books, change the VPN Tunnel exit IP and build a different browsing finger print. Things to change include - 
- Different country and city so each config lands on a different server → distinct exit IP
- Change the User Agent 
A) UA / OS - The User-Agent request header,  navigator.userAgent and Operating system. All say Chrome/149.0.0.0 — a plain desktop Chrome. Rotate between - _UA_LINUX, _UA_WIN, _UA_MAC, _UA_CROS
B) locale -  Rotate between en-US, en-GB, en-CA, en-AU, en-IE, en-NZ 
C) Viewport - The page's window.innerWidth/Height and screen dimensions - Rotate between - 1920×1080, 1536×864, 1728×1117, 1366×768, 2560×1440 etc....


5. For each book 
A) Open the Amazon page and confirm the current price
B) It might be helpful to use the Locall LLM in detecting the current price on the Amazon page
C) If the current price is above the deal price, the deal has expired. Note the deal as current or expired in deals.db
D) After processing 10 books, rotate the VPN tunnel

- [ ] Step 1: Extend app/deals_db.py for price verification: (a) add an additive migration to ensure_schema() (mirroring app/db.py's guarded ALTER TABLE pattern) that adds `deal_status TEXT` (NULL=unchecked, else 'current'|'expired'|'unknown'), `current_price TEXT`, and `verified_at TEXT` to the deal table, idempotent and preserving the 381 existing rows; (b) add `asin_from_amazon_url(url) -> str|None` (reuse the /dp/([A-Z0-9]{10}) rule); (c) add `pending_deals(conn, limit=None) -> list[dict]` returning unverified rows (deal_status IS NULL AND has an ASIN) as {id, asin, amazon_url, deal_price, title}; (d) add `mark_verified(conn, row_id, *, status, current_price, at)` that UPDATEs deal_status/current_price/verified_at; (e) add pure helpers `parse_price_cents(text) -> int|None` (first '$\d+\.\d\d' token, 'free'/'0' -> 0, ranges -> first bound) and `classify_deal(deal_price, current_price) -> (status, current_cents)` with rules: unparseable current -> unknown; current 0/free -> current; unparseable deal -> unknown; current > deal -> expired; else current. ASSUMPTION (recorded): 'deal still live' means Amazon's current price is at or below the stored deal_price; above means expired; if we cannot read a price the deal is 'unknown' (never guessed).
  - files: app/deals_db.py, app/config.py
  - verification: From repo root: `python -m py_compile app/deals_db.py`; then a `python - <<'PY'` (offline) that (i) builds a temp deals.db via store_deals so it has the OLD schema, calls ensure_schema again and asserts the three new columns now exist and old rows are intact, and that a second ensure_schema is a no-op; (ii) calls pending_deals and confirms it returns only the ASIN-bearing unverified rows; (iii) calls mark_verified and confirms deal_status/current_price/verified_at are set and the row leaves pending_deals; (iv) asserts parse_price_cents + classify_deal return the expected (status, cents) for a battery of cases (e.g. deal $2.99 vs current $4.99 -> expired; $2.99 vs $2.99 -> current; $2.99 vs $0.99 -> current; 'Free!' deal vs cur $4.99 -> expired; 'Free!' vs cur 'Free with Kindle Unlimited' -> current; unparseable cur -> unknown). Prints `ok`.
- [ ] Step 2: Add app/fingerprint.py: a browser-fingerprint rotator for anti-bot compliance. Define (a) UA list `_UA_LINUX`, `_UA_WIN`, `_UA_MAC`, `_UA_CROS` — well-formed `Mozilla/5.0 (...) Chrome/149.0.0.0 Safari/537.36` strings whose OS token matches the suffix (Windows NT 10.0; Win64; x64 / X11; Linux x86_64 / Macintosh; Intel Mac OS X 10_15_7 / X11; CrOS x86_64 ...); (b) `LOCALES = ['en-US','en-GB','en-CA','en-AU','en-IE','en-NZ']`; (c) `VIEWPORTS = ['1920x1080','1536x864','1728x1117','1366x768','2560x1440']`; (d) a `Fingerprint` dataclass (user_agent, locale, width, height); (e) `next_fingerprint()` that deterministically rotates through the three lists each call (cycles all UAs then all locales then all viewports), and `fresh_fingerprint(fp=None)` that returns a Fingerprint differing from fp (or the last one) in ALL THREE fields (used after a VPN IP rotation). Pacing/jitter helpers live in the orchestrator, not here. ASSUMPTION (recorded): the four UA strings meet the task's 'Chrome/149.0.0.0 — a plain desktop Chrome' spec and each OS-token variant lands on a distinct navigator/platform profile.
  - files: app/fingerprint.py
  - verification: From repo root: `python -m py_compile app/fingerprint.py`; then a `python - <<'PY'` (offline) asserting: the UA list has exactly 4 entries whose names/OS tokens match the _LINUX/_WIN/_MAC/_CROS suffixes and all contain `Chrome/149.0.0.0`; LOCALES == the 6 given values; VIEWPORTS == the 5 given sizes; calling next_fingerprint() ~200 times covers every (ua, locale, viewport) and yields valid ints; and fresh_fingerprint() returns a fingerprint whose user_agent, locale, and viewport are each different from the argument. Prints `ok`.
- [ ] Step 3: Add app/amazon_price.py: given a Fingerprint and an amazon_url/ASIN, open `https://www.amazon.com/dp/<ASIN>` with Playwright in a context built from the fingerprint (user_agent/locale/viewport, + AutomationControlled-disabled launch args like app.bookbub), detect a block page (reuse the repo's challenge heuristics), extract the current price: fast path via price selectors (`span.a-price span.a-offscreen`, `#corePrice_feature_div .a-price .a-offscreen`, `.priceToPay .a-offscreen`), else optional local-LLM fallback when LLM_MODEL is set — send a bounded trim of the page's innerText to the OpenAI-compatible LLM_BASE_URL chat/completions asking for 'just the current price, e.g. $4.99, or FREE, or NONE' — else None. On a failed/ambiguous read, save the raw HTML to data/diagnostics/ (repo convention) and return None (never raises for a hard-to-read page if the page loaded). Return a small result object (price_text, price_cents via deals_db.parse_price_cents, source 'selector'|'llm'|None, note). Never blocks if the LLM is absent — LLM failure logs a warning and returns None. ASSUMPTION (recorded): amazon.com product pages are fetched as /dp/<ASIN> regardless of the VPN exit country (rotation is for anti-bot, not to switch storefronts), and price selectors are best-effort with the LLM as the optional fallback.
  - files: app/amazon_price.py, app/config.py
  - verification: From repo root: `python -m py_compile app/amazon_price.py`; then a live single-ASIN probe calling amazon_price.read('https://www.amazon.com/dp/B07KVLPYDQ', fingerprint=next_fingerprint()) that prints the price object; success = it returns a non-None price_text for a readable ASIN (or, if the price genuinely can't be read, returns None with a data/diagnostics/ HTML dump and no traceback). LLM_MODEL stays empty (LLM optional).
- [ ] Step 4: Add app/nordvpn.py: a thin, credential-safe wrapper around the `nordvpn` CLI — `login()` (via `nordvpn login --username <NORDVPN_USERNAME> --password <NORDVPN_PASSWORD>`, both read from env or --nord-user/--nord-pass and NEVER written to a committed file), `connect(country, city=None)` (nordvpn connect), `disconnect()`, `ip()` (parse the exit IP from `nordvpn status`), `status()`, and `rotate()` = disconnect() -> pick a (country, city) from a pool that differs from the current one -> connect() -> return the new ip. Also a `python -m app.nordvpn` probe (prints status + exit IP; supports --login). Pacing/anti-bot and the per-10-books invocation live in the orchestrator, not here. ASSUMPTION (recorded): a NordVPN CLI (`nordvpn`) is installed on this machine and supports login/connect/disconnect/status; the supplied account is used only via env/CLI flags and is never committed; country/city pools are configurable via config (NORDVPN_COUNTRIES, e.g. a set of regions) so each rotation lands on a different server/exit IP.
  - files: app/nordvpn.py, app/config.py
  - verification: From repo root: `python -m py_compile app/nordvpn.py`; then `NORDVPN_USERNAME=<env> NORDVPN_PASSWORD=<env> python -m app.nordvpn --login` prints a connected status and a public exit `ip: x.x.x.x`, and a follow-up `python - <<'PY'` that calls `nordvpn.rotate()` and asserts the reported exit IP changed (ip_before != ip_after). Success = the CLI is present, login works, and rotate() changes the exit IP. If the `nordvpn` CLI is absent/not connectable in the build environment, the verification FAILS with the clear underlying error (the task requires a live VPN; this is an environment prerequisite, not something to paper over).
- [ ] Step 5: Add scripts/verify_deals.py — the orchestrator that ties NordVPN + fingerprint + Amazon price read + deals_db classification together. CLI: --limit N (default: all pending), --rotate-every N (default 10, from NORDVPN_ROTATE_EVERY), --db PATH (default DEALS_DB), --nord-user/--nord-pass (env fallback), --check (dry-run: print pending count + config, connect nothing, exit 0). Flow: (1) ensure nordvpn connected (call connect() with a starting country if not); (2) load pending_deals(limit); (3) for each book, in order: build a Fingerprint via next_fingerprint(), call amazon_price.read(), classify_deal(deal_price, current_price), mark_verified(deal_status/current_price/verified_at), persist a resumability mirror to data/verify_progress.json (atomic tmp+replace) and rely on deal_status in DEALS_DB as the source of truth; (4) every --rotate-every books: nordvpn.rotate() (new country+city -> new exit IP) and switch to a fresh_fingerprint() that differs in UA/locale/viewport; (5) pace between books with a random jitter delay (reuse the repo's anti-bot pacing idea; config VERIFY_DELAY_MIN/MAX), and on a detected Amazon block/bot page or hard-to-read result, back off and retry the book up to NORDVPN retry budget, recording a 'unknown' status if it stays unreadable; (6) print per-book lines (id, asin, deal_price -> current_price -> status) and a final summary (current/expired/unknown counts + how many IP rotations). ASSUMPTION (recorded): the operator runs the real 375-book pass over the real data/deals.db; step verification runs a SMALL bounded pass over a TEMP copy of deals.db (--db) so the real DB is not mutated during review, matching the prior steps' convention.
  - files: scripts/verify_deals.py, app/config.py, data/verify_progress.json
  - verification: From repo root: `python -m py_compile scripts/verify_deals.py`; `python scripts/verify_deals.py --check` (exit 0, prints the pending count without connecting); then a bounded LIVE pass on a TEMP copy: `python -c` to copy data/deals.db to a temp path via sqlite backup, then `python scripts/verify_deals.py --db <temp> --limit 2 --rotate-every 2` (exercises one full 2-book loop + one IP rotation), then a `python - <<'PY'` verify block opening <temp> and asserting: exactly 2 rows now have deal_status in {'current','expired','unknown'} with verified_at set and current_price populated (or NULL only when status is 'unknown'), and re-running the same command is a fast no-op (those 2 rows are skipped as verified). The real data/deals.db is left untouched.
- [ ] Step 6: Document the live-deal verification in README.md's BookBub / deals-database area: a 'Verifying deals are still live' subsection describing scripts/verify_deals.py (args --limit/--rotate-every/--db/--check), the per-book Amazon price check vs the stored deal_price (at/below = current, above = expired, unreadable = unknown) with the optional local-LLM fallback, the fingerprint rotation (UA _LINUX/_WIN/_MAC/_CROS Chrome/149, en-* locales, viewport list) and NordVPN exit-IP rotation every N books, the new deal columns (deal_status/current_price/verified_at), the env vars (NORDVPN_USERNAME/NORDVPN_PASSWORD/NORDVPN_ROTATE_EVERY/VERIFY_DELAY_MIN-MAX, credentials env-only never committed), and that deal_status/verified_at in DEALS_DB is the resume cursor. Also note the 10-per-IP rotation and the anti-bot pacing.
  - files: README.md
  - verification: From repo root: `grep -n -i -E 'verify_deals|deal_status|current_price|rotate|NORDVPN|fingerprint|expired' README.md` — success is the grep returns lines in the BookBub/deals section covering the verify command, the deal_status current/expired outcome, the NORDVPN exit-IP + fingerprint rotation every N books, and the env-only NORDVPN credentials.
