# Sortable Deal price & Date of Deal on BookBub Deals tab

Task: 1. Lets make the list sortable by Deal price, and Date of deal
2. When I click on Deal price it should toggle between asending and descending
3. When I click on Date of deal it should toggle between asending and descending
4. The default should be sort by Date of deal, descending - so most recent first

- [ ] Step 1: Data layer for sorting in `app/deals_db.py`. (a) Extend `current_deals()` so each returned dict also carries `deal_price_cents` (int cents from `parse_price_cents(deal_price)`, None when unparseable — so `Free!` → 0) alongside the existing keys; this is safe because only the /bookbub-deals route and test scripts consume it. (b) Add a pure, non-mutating `sort_deals(rows, sort="date", direction="desc")` function (export it in `__all__`) that returns a NEW list sorted by: `sort="date"` → the `date` string key (YYYY-MM-DD, lexicographically chronological) and `sort="price"` → the `deal_price_cents` key, with None (unparseable price) ALWAYS last regardless of direction. `direction="asc"` or `"desc"` reverses the order. This keeps the default (`date`, `desc` = most recent first) identical to today's current_deals behaviour. Assumption recorded: price with None cents sorts last; "Free!" (0 cents) sorts as the cheapest.
  - files: app/deals_db.py
  - verification: Temp-DB script (gitignored data/): seed rows with prices ["$9.99","Free!","$2.99","unparseable"] and varied dates, call current_deals → assert each dict has an int-or-None `deal_price_cents` (Free→0, unparseable→None); call sort_deals(price, asc) → assert order [Free, $2.99, $9.99, unparseable(last)]; sort_deals(price, desc) → [$9.99, $2.99, Free, unparseable(last)]; sort_deals(date, desc) → newest-first and (date, asc) → oldest-first; assert input list is NOT mutated. Command: `python -m py_compile app/deals_db.py && python data/_verify_sort_tmp.py` prints OK and exits 0.
- [ ] Step 2: Wire sort query params into the /bookbub-deals route in `app/routes/pages.py`. Add GET params `sort` (whitelist "price"|"date", default "date") and `dir` (whitelist "asc"|"desc", default "desc" — named `direction` in the handler with `Query(..., alias="dir")` so the URL stays `?dir=` without shadowing the builtin). The handler calls `deals_db.current_deals(conn)` then `deals_db.sort_deals(rows, sort=s, direction=d)`, paginates with `extra_query={"sort": s, "dir": d}` so page-2/3/4 links preserve the active sort, and passes `sort` and `dir` into the template context. Sorting happens on the full list before pagination so every page is consistently ordered. No template controls yet — the route is reachable and correct but not yet user-clickable.
  - files: app/routes/pages.py
  - verification: Temp-DB-seeded TestClient (gitignored data/ script, set throwaway WISHLIST_*/DEALS_DB env before importing app.main, as in prior steps): seed current deals with known prices/dates; assert `GET /bookbub-deals` (default) prints the most-recent-first order; `GET /bookbub-deals?sort=price&dir=asc` returns 200 and the HTML renders titles in ascending-price order (assert relative positions in the tbody via html.index ordering, cheapest before expensive); `GET /bookbub-deals?sort=price&dir=desc` reverses it; an invalid `?sort=bogus` falls back to the default date-desc without error. Command exits 0 = pass.
- [ ] Step 3: Make the "Deal price" and "Date of Deal" column headers clickable and toggle direction in `app/templates/bookbub_deals.html` (uses `sort`/`dir` context vars from step 2). Header cells render as links to `/bookbub-deals?sort=<col>&dir=<next>` where the next direction is: if that column is the active sort, toggle (price: desc if currently asc else asc; date: asc if currently desc else desc); if not the active sort, use the column's default (price→`asc`, date→`desc` — so date stays most-recent-first by default per requirement 4). Show a small ▲/▼ arrow next to the label on the currently-active sortable column to make the sort state obvious. Title, Author, and Regular price headers stay plain text. Sort links include no `page` so clicking one returns to page 1 of the new order; pagination already preserves sort/dir via extra_query.
  - files: app/templates/bookbub_deals.html
  - verification: TestClient (temp-deals-db script as in step 2): `GET /bookbub-deals` (default, date desc) → the Date of Deal header anchor is `/bookbub-deals?sort=date&dir=asc` with a ▼ arrow and class indicating active, and the Deal price header anchor is `/bookbub-deals?sort=price&dir=asc`; `GET /bookbub-deals?sort=price&dir=asc` → the Deal price header anchor toggles to `?sort=price&dir=desc` (▲ arrow, active) and the Date header is `?sort=date&dir=desc`; `GET /bookbub-deals?sort=price&dir=desc` → Deal price anchor is `?sort=price&dir=asc`. Assert the exact hrefs/arrows. Command exits 0 = pass.
- [ ] Step 4: Document the sortable columns in `README.md` under the existing /bookbub-deals Pages bullet: add that the Deal price and Date of Deal columns are clickable sort headers (click to cycle asc/desc; clicking a non-active column switches to it with its default — price ascending, date descending), that the default order is Date of Deal descending (most recent first), and that the URL query params are `sort=price|date` and `dir=asc|desc` (preserved across pages). No code change.
  - files: README.md
  - verification: `grep -q "sort=price" README.md` and `grep -q "sort=date" README.md` both exit 0 (the new sort params are documented) and the app still imports cleanly (`.venv/Scripts/python.exe -c "from app.main import app; print('ok')"` from the repo root prints ok / exit 0).
