# BookBub deals database + Amazon links + ownership audit

Task: Lets modify this. The script is now able to generate a list of the books on the page with links to the books and the price of the books. The Link saved at this time is the bookbub link. 
1. Lets create another databse to store the books on the bookbub page, deals_database
2. Lets change the link saved with the book, the link should be the Amazon kindle page link for the book. 
3. Books without an Amazon Kindle page link will have no link saved. This will be noted in the deals_database (i.e. note in the deals_database that no amazon link exists for the book).
4. Search for the book in the first database (grimmory.db) of all my books that we created earlier, if it is in the grimmory.db (i.e. if I own the book) note this in the deals_database
5. In #3 and #4 the books are left in the database to allow an audit of accuracy

- [ ] Step 1: Add a DEALS_DB setting to app/config.py in the BookBub section, following the env-var pattern: DEALS_DB = Path(os.environ.get('DEALS_DB', DATA_DIR / 'deals.db')). ASSUMPTION (recorded): the new 'deals database' is an independent SQLite file data/deals.db (already gitignored via the data/ rule), separate from wishlist.db and grimmory.db. No other config changes this step.
  - files: app/config.py
  - verification: From repo root: `python -c "import app.config as c; assert c.DEALS_DB.name=='deals.db' and c.DEALS_DB.parent.name=='data'; assert c.GRIMMORY_DB.name=='grimmory.db'; print('ok')"` — success is it prints `ok` (DEALS_DB resolves to the repo's data/deals.db and GRIMMORY_DB is still available for the ownership lookup).
- [ ] Step 2: Extend app/bookbub.py to capture the Amazon Kindle page link per deal. Add `amazon_url: str | None = None` (and `original_price: str = ''`) to the Deal dataclass, and update parse_deals to extract, per deal card, the Amazon CTA/retailer link (e.g. the primary `.book-card-action-deal` action and/or `.retailer-button a[href*='amazon']`). Where the href is a BookBub click-tracker/intermediate (bookbub.com or outbound.bookbub.com) rather than a direct amazon.com URL, resolve it to the final Amazon page with a follow_redirects httpx GET and store that as amazon_url; if a card has no Amazon retailer/action link at all, amazon_url stays None. ASSUMPTION (recorded): the daily-deals page has the Amazon link on the card itself (it has .book-card-action-deal / .retailer-button elements), so no per-book page fetch is needed; the builder MUST inspect the live rendered DOM (re-run the Playwright fetch, save the HTML to data/diagnostics/) and pin the exact selectors before finalising, matching this repo's scrape-selectors-on-drift convention. Keep Deal.url as the BookBub book page (audit). Update the __main__ probe to also print the resolved Amazon URL (or '(no amazon link)') per deal.
  - files: app/bookbub.py
  - verification: From repo root (live): `python -m app.bookbub --link '<the sample outbound link>' --date 20260827` — success is it exits 0 and the printed output shows at least one resolved amazon.com URL for some deal row (and '(no amazon link)' markers where applicable). Also `python -m py_compile app/bookbub.py` passes.
- [ ] Step 3: Add app/deals_db.py — schema + ownership lookup + idempotent upsert for the deals database. Schema for a `deal` table: id (PK AUTOINCREMENT), date (YYYYMMDD), title (NOT NULL), author, deal_price, original_price, bookbub_url, amazon_url (NULL when the book has no Amazon edition), no_amazon_link (INTEGER, 1 when amazon_url is NULL), owned_in_grimmory (INTEGER: 1 owned / 0 not owned / NULL = grimmory.db unavailable), audited_at (ISO timestamp); plus a UNIQUE index on (date, bookbub_url). Functions: `normalise(text)` (lowercase, collapse whitespace, strip common punctuation) for matching; `owned_lookup(deals, grimmory_path) -> dict` that reads grimmory.db's `book(title, author)` rows and maps each deal's bookbub_url to owned=1 if any grimmory book matches on normalised title AND normalised author, else 0 (returns None for a deal if grimmory.db is missing — audit left NULL, not a hard failure); and `upsert_deals(conn, deals, date, owned_map, audited_at)` that INSERTs each deal and, on (date, bookbub_url) conflict, UPDATEs it (so re-running the same date refreshes that day's rows and never grows duplicates; past dates' rows are never deleted — audit retention). ASSUMPTION (recorded): ownership is an approximate normalised-title+author match, kept in the DB so a human can audit accuracy (per the task).
  - files: app/deals_db.py
  - verification: Offline (no network): a `python - <<'PY'` block that (a) creates a temp grimmory.db with a few `book` rows, (b) builds a temp deals.db and runs owned_lookup + upsert_deals on a small synthetic Deal list mixing owned/not-owned and amazon-url/no-amazon deals, then (c) asserts the stored rows have the expected amazon_url / NULL, no_amazon_link, owned_in_grimmory, and audited_at, and that upserting the same (date, bookbub_url) twice leaves the row count unchanged. Prints `ok`.
- [ ] Step 4: Wire the deals database into scripts/build_bookbub_deals.py so one run both stores the deal records and regenerates booklist.md. After `bookbub.fetch_daily_deals(...)`: open DEALS_DB, create the schema, compute owned_lookup from GRIMMORY_DB (if grimmory.db is absent, owned stays NULL), and upsert all deals for the date with audited_at=now; print a summary (X deals stored, Y owned, Z no-amazon-link). Also change `_deals_to_markdown` to link each title to its resolved Amazon URL instead of the BookBub page (plain title with no link when amazon_url is None, matching 'no link saved'); the BookBub url stays only in the deals DB. The atomic-write and optional-LLM behaviour are unchanged.
  - files: scripts/build_bookbub_deals.py, app/config.py, app/deals_db.py
  - verification: From repo root (live): `python scripts/build_bookbub_deals.py --link '<the sample outbound link>' --date 20260827` run twice, then `python -c "import sqlite3; c=sqlite3.connect('data/deals.db'); n=c.execute('SELECT COUNT(*) FROM deal').fetchone()[0]; cols={r[1] for r in c.execute('PRAGMA table_info(deal)')}; have_am=c.execute('SELECT COUNT(*) FROM deal WHERE amazon_url IS NOT NULL').fetchone()[0]; noam=c.execute('SELECT COUNT(*) FROM deal WHERE no_amazon_link=1').fetchone()[0]; own=c.execute('SELECT COUNT(DISTINCT owned_in_grimmory) FROM deal').fetchone()[0]; assert n>0 and {'date','title','author','deal_price','bookbub_url','amazon_url','no_amazon_link','owned_in_grimmory','audited_at'}<=cols and have_am>=1 and own>=1; print('rows',n,'have_amazon',have_am,'no_amazon',noam,'owned_distinct',own)"` — success is the script exits 0 both times, the check prints rows>0 with amazon_url populated and owned_in_grimmory populated, and the row count is identical across the two runs (re-run upserts, no duplicate growth). Also `grep -c -i 'amazon' booklist.md` is > 0.
- [ ] Step 5: Document the deals database and the link change in README.md's existing 'BookBub daily ebook deals' section: the new deals database (data/deals.db, gitignored), its deal table columns (date/title/author/deal_price/bookbub_url/amazon_url/no_amazon_link/owned_in_grimmory/audited_at), the DEALS_DB env var, that booklist.md now links each title to the Amazon Kindle page (plain title when no Amazon edition exists), and how re-runs upsert per date and keep history for audit. Mention the ownership lookup against grimmory.db is an approximate title+author match retained for audit.
  - files: README.md
  - verification: From repo root: `grep -n -i -E 'deals\.db|deals database|DEALS_DB|amazon_url|no_amazon_link|owned_in_grimmory|Amazon' README.md` — success is the grep returns lines in the BookBub section covering data/deals.db (DEALS_DB), the amazon link behaviour, and the no_amazon_link / owned_in_grimmory audit columns.
