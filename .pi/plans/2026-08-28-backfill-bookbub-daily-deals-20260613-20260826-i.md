# Backfill BookBub daily deals (20260613–20260826) into deals.db

Task: Lets extend the days covered.
1. We have processed the deals for the 8/27/26 BookBub page
2. Lets go back day by day till we reach 6/13/26 and process each BookBub deals page like the 8/27/26 page
3. For each day, open the BookBub deals page and extract the BookBub deals
4. Save the details for each deal in deals.db. 
5. Books without an Amazon Kindle page link will have no link saved. This will be noted in deals.db (i.e. note in deals.db that no amazon link exists for the book).
6. Search for the book in the first database (grimmory.db) of all my books that we created earlier, if it is in the grimmory.db (i.e. if I own the book) note this in the deals_database
7. Monitor for cloudflare protection

- [ ] Step 1: Refactor the single-day storage path into a reusable function and switch the existing builder to it. Add `store_deals(deals, date, *, deals_path, grimmory_path, audited_at=None) -> tuple[int,int,int]` to app/deals_db.py: it opens deals_path (deals_db.connect), ensure_schema, owned_lookup(deals, grimmory_path), upsert_deals with audited_at (datetime.now().isoformat(timespec="seconds") when not given), commits, closes, and returns (stored, owned, no_amazon) — the exact logic currently in scripts/build_bookbub_deals.py's private `_store_deals`. Then delete that private `_store_deals` and have `scripts/build_bookbub_deals.py` call `deals_db.store_deals(deals, date, deals_path=DEALS_DB, grimmory_path=GRIMMORY_DB)` from the same spot, keeping its log line and behavior identical. ASSUMPTION (recorded): ownership/amazon/no-amazon handling is unchanged from the completed prior PBR; this is a pure extraction so both the single-day builder and the new backfill driver share one storage path.
  - files: app/deals_db.py, scripts/build_bookbub_deals.py
  - verification: From repo root: `python -m py_compile app/deals_db.py scripts/build_bookbub_deals.py`, then a `python - <<'PY'` that builds a temp deals.db + temp grimmory.db, calls deals_db.store_deals twice on the same (date, bookbub_url) synthetic list and asserts (a) row count stays the same (idempotent upsert), (b) returned (stored, owned, no_amazon) counts are correct, (c) audited_at/no_amazon_link/owned_in_grimmory columns are populated as expected. Success = py_compile passes and the block prints `ok`.
- [ ] Step 2: Add a session-reusing batch fetcher `fetch_daily_deals_many(dates, link, headless=True) -> dict[str|date, list[Deal]]` to app/bookbub.py (additive — leave `fetch_daily_deals`/`fetch_daily_deals_playwright` unchanged). It opens ONE Playwright browser, logs in once via the outbound link (clearing the Cloudflare challenge), then for each date navigates to `_deals_url(date)`, waits past any re-challenge (`_load_deals`/`_wait_past_challenge`), and parses the page. Each date is best-effort: a date that re-challenges or has no deal cards yields an empty list for that date (recorded, not an abort). Reuse the existing `_on_daily_deals_for_date` optimization (parse the landing page as-is when the first requested date equals the link's date). After collecting, it calls the shared `resolve_amazon_urls` on each date's deals and returns the dict keyed by date. ASSUMPTION (recorded): BookBub serves each historical day at `/ebook-deals/daily-deals?date=YYYYMMDD` within one logged-in session (the existing per-date fetcher already relies on this for the navigate-when-date-differs case); days no longer served come back with no deal cards and are recorded as empty.
  - files: app/bookbub.py
  - verification: From repo root: `python -m py_compile app/bookbub.py`; then a live probe `python - <<'PY'` that calls `bookbub.fetch_daily_deals_many(['20260826','20260825'], link='<a valid outbound login link>')` and asserts the returned dict has both date keys, each with a non-empty (or legitimately empty) deal list, and that the 20260826 set differs from the 20260825 set (proves per-date `?date=` navigation works and isn't just returning the landing page). Success = py_compile passes and the probe prints per-date counts with the two date-sets cardinally different.
- [ ] Step 3: Add the backfill driver `scripts/backfill_bookbub_deals.py` plus its config knobs. Config (app/config.py, BOOKBUB_* convention): `BOOKBUB_BACKFILL_START` (default 20260826), `BOOKBUB_BACKFILL_END` (default 20260613), `BOOKBUB_BACKFILL_CHUNK` (default 5 — dates per login session before re-login to bound session length), `BOOKBUB_BACKFILL_DELAY` (default 3 — seconds between dates in a session, jittered), `BOOKBUB_BACKFILL_BACKOFF` (default 30 — seconds to sleep between chunks), `BOOKBUB_BACKFILL_PROGRESS` (default data/backfill_progress.json, gitignored via data/). The driver: (a) computes the descending date list [end..start] inclusive; (b) on startup seeds its progress `done` set both from `BOOKBUB_BACKFILL_PROGRESS` (if present) AND from `SELECT DISTINCT date FROM deal` in DEALS_DB (so 20260827 and any prior rows are treated as done); (c) skips dates with status `ok`/`empty`; (d) walks the remaining dates in chunks, calling `bookbub.fetch_daily_deals_many(chunk_dates, link)` and `deals_db.store_deals(deals, date, deals_path=DEALS_DB, grimmory_path=GRIMMORY_DB)` per date (dates returning no deals are still recorded as `empty` so they're not retried every run; dates where the login is dead abort the whole run with a clear message, mirroring the repo's login-expired abort); (e) CLOUDFLARE MONITORING: a date whose page still shows the challenge after the wait is recorded as status `challenge` (retriable later, NOT `empty`), consecutive challenge/error dates trigger the backoff sleep, and every challenge is printed loud + written to the progress file; (f) writes the progress file atomically (tmp + os.replace) after each date (repo convention) so a killed run resumes; (g) `--link` required, `--start/--end` override the env defaults, `--dry-run` prints the date list and would-do without fetching; (h) prints a per-date line and a final summary (dates ok / empty / challenge / error, total deals stored, total owned, total no-amazon). It does NOT write booklist.md (that single-day report stays owned by build_bookbub_deals.py); the backfill only populates deals.db.
  - files: app/config.py, scripts/backfill_bookbub_deals.py, data/backfill_progress.json
  - verification: From repo root, run `python scripts/backfill_bookbub_deals.py --link '<valid login link>' --start 20260826 --end 20260824 --dry-run` (prints the 3 dates, exit 0), then for real `python scripts/backfill_bookbub_deals.py --link '<valid login link>' --start 20260826 --end 20260824`; success = exit 0, `data/deals.db` now has distinct dates incl. 20260826/20260825/20260824 (plus the existing 20260827), `data/backfill_progress.json` records those dates with count/status/audit, and re-running the same command is a fast no-op (dates already `ok`) that leaves the deal row counts unchanged, then `python -c "import sqlite3;c=sqlite3.connect('data/deals.db');d=[r[0] for r in c.execute('SELECT DISTINCT date FROM deal')];n=c.execute('SELECT COUNT(*) FROM deal').fetchone()[0];assert '20260824' in d and '20260826' in d and n>=1;print('dates',len(d),'rows',n)"` prints success.
- [ ] Step 4: Document the backfill in README.md within/next to the existing 'BookBub daily ebook deals' section: the new `scripts/backfill_bookbub_deals.py` command and its `--link/--start/--end/--dry-run` args, the new `BOOKBUB_BACKFILL_*` env vars (start/end/chunk/delay/backoff/progress path), that it populates data/deals.db only (not booklist.md) using the same store_deals path (ownership vs grimmory.db, no_amazon_link for missing Amazon links), that it is idempotent/resumable via data/backfill_progress.json (atomic) + DEALS_DB distinct dates, and how Cloudflare re-challenges are monitored (status `challenge` = retriable + backoff; empty pages recorded as `empty`; a dead login aborts cleanly).
  - files: README.md
  - verification: From repo root: `grep -n -i -E 'backfill_bookbub_deals|BOOKBUB_BACKFILL|backfill_progress|deals\.db|challenge' README.md` — success is the grep returns lines in the BookBub section covering the backfill command, the `BOOKBUB_BACKFILL_*` env vars, the data/backfill_progress.json resume file, and that the backfill writes deals.db and monitors Cloudflare challenges.
