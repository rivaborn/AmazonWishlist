# BookBub daily ebook deals → booklist.md

Task: The next step
1. I receive an email from Bookbub every day with a link to a list of Amazon ebook deals
2. This is a sample link "https://outbound.bookbub.com/ss/c/u001.gxRWG20Al_x-rh8lmqMKHuo08ccPcZk5sNgYMNpbN2nPKw-YeQ6zRBZ3xLi54EVTQePjMkK7TTdkwSTK7clkxpFaK7X439a7NfpcnoigACC8JRfVAD_gG7gafqxYRzIRb00UU45cJfZB3quToPH9PqGHgUvNx_z7iweUC-nbIdlfzrX4stLRDzH6FeQHDSPynETdsBVu65FiCBIVIjasDYQUMIRdNsdEGqYtYu5pxZ5tLOlk_gbR4OETrjf2paN9RviL-NvqMYWk6ysMkgQCTM0JRKsPzRqbF-LCQ8uo1-U/4tj/hLNDJapjTqm_mdcPVoxQfQ/h97/h001.UrwWbRIAwl0cISXXAmTm3U8Ej969VmLxV-dOUh_FLek"
3. The link logs me in automatically on the BookBub website
4. Once logged in each day is represented by a date at the end of a url link e.g. for Today (8/27/26) the url link is "https://www.bookbub.com/ebook-deals/daily-deals?date=20260827"
5. Use the sample link above to log into BookBub 
6. Write a script that, after logging in, reads the webpage and generates a list of the ebooks with the deal price
6. You can use the local LLM models to process the webpage if that is useful
7. Save the list of ebook deals to "C:\Coding\rivaborn\AmazonWhishlist\booklist.md"

- [ ] Step 1: Add BookBub settings to app/config.py following the env-var pattern: BOOKBUB_LOGIN_LINK (default '' — the daily email's outbound auto-login link is a signed session token, so it is supplied at run time via --link/env, never committed); BOOKBUB_DAILY_DEALS_BASE (default 'https://www.bookbub.com/ebook-deals/daily-deals'); BOOKBUB_DATE_FORMAT (default '%Y%m%d'); BOOKBUB_OUTPUT (default BASE_DIR/'booklist.md'); plus optional local-LLM settings LLM_BASE_URL (default 'http://192.168.1.40:11430/v1'), LLM_MODEL (default '' = disabled), LLM_TIMEOUT. ASSUMPTION (recorded): the task's target path 'C:\Coding\rivaborn\AmazonWhishlist\booklist.md' contains a typo — the working repo folder is 'AmazonWishlist', so the deals file is written to the repo root (BASE_DIR/booklist.md). The deals file is a daily generated report, not part of the app; no gitignore change.
  - files: app/config.py
  - verification: From repo root: `python -c "import app.config as c; assert c.BOOKBUB_OUTPUT.name=='booklist.md' and c.BOOKBUB_OUTPUT.parent==c.BASE_DIR; assert c.BOOKBUB_DAILY_DEALS_BASE=='https://www.bookbub.com/ebook-deals/daily-deals'; assert c.BOOKBUB_LOGIN_LINK==''; print('ok')"` — success is it prints `ok` (confirms output resolves to the repo-root booklist.md and that no session token was hardcoded).
- [ ] Step 2: Add app/bookbub.py — a client module plus parser reusing the repo's httpx + selectolax conventions. Define a `Deal` dataclass (title, author, price, url). `outbound_session(link) -> httpx.Client`: follow the outbound.bookbub.com redirect chain with follow_redirects=True + a persistent cookie jar and the repo's browser-like USER_AGENT headers, landing on bookbub.com logged in. `fetch_daily_deals(date='%Y%m%d') -> list[Deal]`: GET the daily-deals page (?date=YYYYMMDD) with the session and parse the deal cards with selectolax (deal title, author, deal-price, link). If the httpx path yields an auth-wall or zero deals, fall back to Playwright headless chromium (already installed locally) to navigate the outbound link then the deals page and extract from the rendered DOM — mirroring the repo's httpx<->playwright fallback philosophy. Raise a `BookbubError` on any failure (HTTP status, no deals, unreachable). Add a `__main__` probe that takes `--link` (required) and `--date` (default today), prints each deal with price, and exits non-zero if the list is empty. Note: step requires external internet from the build host and a valid fresh outbound link (the task premise).
  - files: app/bookbub.py
  - verification: From repo root, with the live outbound link supplied: `python -m app.bookbub --link '<the sample outbound link from the task>' --date 20260827` — success is it prints at least one deal row containing a title and a `$price` and exits 0 (proves auto-login + page fetch + parse all work end to end).
- [ ] Step 3: Add scripts/build_bookbub_deals.py — the runnable one-off that produces the deliverable. Bootstrap sys.path to the repo root (same as scripts/build_grimmory_db.py). CLI: `--link` (required; else BOOKBUB_LOGIN_LINK env), `--date` (default today), `--out` (default config.BOOKBUB_OUTPUT). It fetches the day's deals via app.bookbub and writes a markdown report: a heading with the date plus one row per deal (title with its link, author, deal price). Write atomically (tmp + os.replace). Optional, OFF-by-default enhancement: if `--llm-model`/LLM_MODEL is set, post-process the parsed rows through the local LLM gateway (OpenAI-compatible POST to LLM_BASE_URL) to normalize the list — this is optional and must never block the write when a model is missing/unreachable (the non-LLM path is the deliverable). Exit non-zero with a clear message if fetch/parse yields nothing or a required arg is missing.
  - files: scripts/build_bookbub_deals.py
  - verification: From repo root: `python scripts/build_bookbub_deals.py --link '<the sample outbound link from the task>' --date 20260827` then `grep -c -E '\$[0-9]+\.[0-9]{2}' booklist.md` — success is the script exits 0 and the grep prints a positive integer (booklist.md was written at the repo root with at least one line carrying a dollar price), and `test -s booklist.md` confirms the file is non-empty.
- [ ] Step 4: Document the BookBub daily-deals pull in README.md, in the existing operator-doc style: a section covering scripts/build_bookbub_deals.py, the output file booklist.md (repo root, markdown list of title/author/price/date), how the outbound email link auto-logs BookBub in (BOOKBUB_LOGIN_LINK / --link), the --date flag and daily-deals URL format, and the BOOKBUB_* and optional LLM_* env vars (documenting names/defaults, NOT embedding the session link). Mention that the local LLM gateway is an optional normalization step, off by default.
  - files: README.md
  - verification: From repo root: `grep -n -i 'bookbub' README.md` — success is the grep returns a section containing lines for build_bookbub_deals.py, booklist.md, the daily-deals URL, and the BOOKBUB_* env var names.
