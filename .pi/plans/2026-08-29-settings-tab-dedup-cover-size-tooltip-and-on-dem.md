# Settings tab, dedup, cover-size/tooltip, and on-demand VPN

Task: Updates to make
1. When updating the deals list from the boookbub daily deals website, remove any duplicates created by the new books
2. Add a drop down to the bookbub deals page to increase the size of the cover. Options - 1X, 2X, 4X
3. Add the tooltip text to hovering over the cover
4. When restarted, the app should not update from bookbub or grimmory. Updating takes place only once a day at the scheduled time
5. When not in use, the VPN should not be connected

5. Add a settigs tab to configure
A) Daily time when the app checks the bookbub deals for the day, updates my owned books and removes duplicates (the same time for all)
B) Daily time when the app starts checking amazon wishilists for deals (currently set to 12 midnight)
C) Default size of covers on the bookbub deals list. Options - 1X, 2X, 4X

- [ ] Step 1: Add runtime-persisted settings storage so the Settings tab can configure daily times and cover size. (a) app/db.py: add a `settings(key TEXT PRIMARY KEY, value TEXT)` table to SCHEMA and to _migrate (CREATE TABLE IF NOT EXISTS, idempotent; ignored by export_catalog so it never mirrors). (b) app/settings.py (new): `get(key, default)`, `get_int(key, default)`, `set(key, value)` (None deletes/resets to default), `all()` — all via db.connect() so the smoke test's db.DB_PATH patch redirects them like every other query. (c) app/config.py: add `BOOKBUB_COVER_SIZE_OPTIONS = ['1x','2x','4x']`, `BOOKBUB_COVER_SIZE_DEFAULT = '1x'`, `BOOKBUB_HOUR_DEFAULT = 18`, `BOOKBUB_MINUTE_DEFAULT = 0` (the bookbub daily default stays 18:00; the wishlist-scrape default stays the existing SCRAPE_HOUR/MINUTE env defaults). Assumption recorded: env vars remain the DEFAULTS; a present setting overrides them at the point of use, and settings are primary-only (never mirrored).
  - files: app/db.py, app/settings.py, app/config.py
  - verification: .venv/Scripts/python.exe -m py_compile app/db.py app/settings.py app/config.py && set WISHLIST_DB to a temp file and run a data/_verify_settings_tmp.py harness that init_db()s, then app.settings.set/get/get_int/all round-trips (set a string, an int, delete-by-None, overwrite-on-conflict) and asserts the settings table exists — prints ALL CHECKS PASSED. Then PYTHONPATH=. .venv/Scripts/python.exe scripts/_smoke.py still ends 'ALL CHECKS PASSED'.
- [ ] Step 2: Make the app's scheduler read the daily times from settings and drive the BookBub run, so req 4 (no bookbub/grimmory update on restart — only the configured daily time) and req 6A/6B (configurable times) are met. In app/scheduler.py: (a) scrape time = settings.get_int('scrape_hour', config.SCRAPE_HOUR)/'scrape_minute'; bookbub time = settings.get_int('bookbub_hour', config.BOOKBUB_HOUR_DEFAULT)/'bookbub_minute'. (b) On the PRIMARY, besides `daily_scrape`, register a `bookbub_daily` cron job (max_instances=1, coalesce=True) whose callable `run_bookbub()` runs `subprocess.run(['/usr/bin/sudo','-n','/usr/bin/systemctl','start','amazon-wishlist-bookbub.service'])` (the existing one-shot unit, which places bookbub_daily inside the wlvpn netns and tears the VPN down when done — ExecStopPost added in step 6); a non-systemd dev box logs a warning. (c) Add `reschedule_jobs()` that re-triggers both cron jobs from current settings (no-op off-scheduler or on a secondary).(d) Secondary stays sync-only. Requirement 4 is satisfied structurally: a CronTrigger never fires at startup — the bookbub job's next_run_time is always in the future after start_scheduler(), so a restart never triggers a bookbub/grimmory update. Assumption: triggering the unit via the scoped sudoers rule (provisioned in step 6) is the intended bridge because only systemd can place a process inside the netns.
  - files: app/scheduler.py
  - verification: .venv/Scripts/python.exe -m py_compile app/scheduler.py && a data/_verify_scheduler_tmp.py harness (WISHLIST_DB+WISHLIST_PROGRESS+WISHLIST_LOG to temp, init_db, start_scheduler on PRIMARY) asserts jobs 'daily_scrape' and 'bookbub_daily' exist, bookbub_daily.next_run_time is strictly IN THE FUTURE (proving no bookbub run at startup), then set settings scrape_hour/bookbub_hour -> call scheduler.reschedule_jobs() -> asserts the job's next_run_time moved to the new time; then stop_scheduler — prints ALL CHECKS PASSED. Then PYTHONPATH=. .venv/Scripts/python.exe scripts/_smoke.py still ends 'ALL CHECKS PASSED'.
- [ ] Step 3: Add the Settings page and its mutating API (req 6). (a) app/routes/pages.py: GET /settings — primary-only (secondary -> 403), reads current settings plus the config defaults (scrape/bookbub HH:MM, cover size) into the context. (b) app/routes/api.py: POST /api/settings — primary-only via _require_primary; accepts any of scrape_time / bookbub_time (HH:MM, validated 0-23/0-59) / cover_size (must be in BOOKBUB_COVER_SIZE_OPTIONS); persists via app.settings, calls scheduler.reschedule_jobs(); returns 303 -> /settings. Only supplied fields are written, so the cover-size dropdown (step 4) can reuse it. (c) app/templates/settings.html (new): a form with the two time inputs + cover-size select and current values. (d) app/templates/base.html: add a Settings nav link gated `{% if not readonly %}` (same as Login), so a mirror never sees it.
  - files: app/routes/pages.py, app/routes/api.py, app/templates/settings.html, app/templates/base.html
  - verification: .venv/Scripts/python.exe -m py_compile app/routes/pages.py app/routes/api.py && a data/_verify_settings_page_tmp.py harness with TestClient (primary) asserts GET /settings -> 200 and contains the three fields, POST /api/settings {scrape_time:'01:30',bookbub_time:'19:15',cover_size:'2x'} -> 303 and app.settings reflects the values and the scheduler 'bookbub_daily'/'daily_scrape' next_run_time changed; then set config.ROLE='secondary' -> GET /settings 403 and POST /api/settings 403 — prints ALL CHECKS PASSED. Then _smoke.py still ends 'ALL CHECKS PASSED'.
- [ ] Step 4: Add the cover-size dropdown and the cover hover tooltip on the BookBub Deals tab (req 2, req 3, and the display half of req 6C). (a) app/routes/pages.py: bookbub_deals_page reads `settings.get('cover_size', BOOKBUB_COVER_SIZE_DEFAULT)` and adds `cover_size`, `cover_size_options`, and `cover_size_default` to the context. (b) app/templates/bookbub_deals.html: add a cover-size <select> (1x/2x/4x, current value selected) in the toolbar next to the per-page dropdown whose onchange POSTs {cover_size} to /api/settings then reloads the page; render the cover <img> as `class="deal-cover size-{{ cover_size }}"`; add `title="{{ r.description }}"` to the cover <img> (requirement 3 — currently only the title link has it). (c) app/static/style.css: per-size widths (.deal-cover.size-1x/2x/4x, e.g. 64/128/256px) and widen .cover-col per size so the larger images are not clipped.
  - files: app/routes/pages.py, app/templates/bookbub_deals.html, app/static/style.css
  - verification: .venv/Scripts/python.exe -m py_compile app/routes/pages.py && a data/_verify_cover_size_tmp.py harness: seed a temp deals.db with one current deal that has a cover filename + description and write that file into a temp covers dir; set DEALS_DB/DEALS_COVERS_DIR and app.settings.set('cover_size','4x'); TestClient GET /bookbub-deals -> body contains 'size-4x', the cover filename as src, and title=description on the cover <img>; with the setting cleared -> 'size-1x' — prints ALL CHECKS PASSED. Then _smoke.py still ends 'ALL CHECKS PASSED'.
- [ ] Step 5: Remove duplicates created by newly-fetched books in the daily BookBub run (req 1). In scripts/bookbub_daily.py, after the (1b) owned_in_grimmory refresh block, add a (1c) block that reuses the existing deals_db.deduplicate(conn) + commit inside a try/except that logs and continues on failure (best-effort, like the owned refresh) — a book re-featured on a later date creates a second row for the same identity and deduplicate collapses to the most recent deal per book (deals_db.book_identity: ASIN, else normalised title+author). Runs in the same daily cycle at the configured bookbub time, so fetch + owned-refresh + dedup all happen together (req 6A 'same time for all').
  - files: scripts/bookbub_daily.py
  - verification: .venv/Scripts/python.exe -m py_compile scripts/bookbub_daily.py && a data/_verify_dedup_daily_tmp.py harness seeds a temp deals.db with two rows sharing one ASIN identity at different dates plus one distinct row, runs the exact deduplicate(conn)+commit sequence bookbub_daily adds, and asserts the older duplicate is deleted and the distinct row remains; also assert via grep that bookbub_daily.py contains a `deals_db.deduplicate` call — prints ALL CHECKS PASSED. Then _smoke.py still ends 'ALL CHECKS PASSED'.
- [ ] Step 6: VPN up only while the daily BookBub run is in use (req 5) and point bookbub scheduling at the app (req 4/6A enforcement). (a) amazon-wishlist-vpn.service: remove `WantedBy=multi-user.target` (and the [Install] section) so it is NOT enabled/started at boot — it is now started on demand by the bookbub unit's Requires=. (b) amazon-wishlist-bookbub.service: add `ExecStopPost=/usr/bin/sudo -n /usr/bin/systemctl stop amazon-wishlist-vpn.service` so the tunnel is torn down after the run (including on failure/crash of the oneshot); update the header comment (no longer timer-driven at 18:00 — app-scheduled). (c) Delete amazon-wishlist-bookbub.timer (the app now schedules the run). (d) scripts/install_systemd.sh: `systemctl disable --now amazon-wishlist-vpn.service` and stop it if running; remove the install/start of amazon-wishlist-bookbub.timer; drop the install's best-effort `systemctl start amazon-wishlist-bookbub.service` (it would block the deploy for the whole cycle); and provision /etc/sudoers.d/amazon-wishlist (m=0440, validated with `visudo -cf`) granting the wishlist user NOPASSWD scoped access — mirroring the pre-existing vpn_verify.sh scoped-sudoers precedent — to exactly: `/usr/bin/systemctl start amazon-wishlist-bookbub.service` and `/usr/bin/systemctl stop amazon-wishlist-vpn.service`. Assumption: this scoped rule is the acceptable least-privilege bridge (two exact commands, same repo pattern) for the app (wishlist user) to start the netns-bound unit and to tear the VPN down.
  - files: amazon-wishlist-vpn.service, amazon-wishlist-bookbub.service, amazon-wishlist-bookbub.timer, scripts/install_systemd.sh
  - verification: bash -n scripts/install_systemd.sh && static greps: amazon-wishlist-vpn.service has NO 'WantedBy=multi-user.target' and no [Install] section; amazon-wishlist-bookbub.service contains 'ExecStopPost=...stop amazon-wishlist-vpn.service'; install_systemd.sh contains 'systemctl disable --now amazon-wishlist-vpn.service' (or equivalent), 'systemctl disable amazon-wishlist-bookbub.timer', the /etc/sudoers.d/amazon-wishlist write plus 'visudo -cf', and NO 'systemctl enable amazon-wishlist-vpn'. Confirm the timer file is deleted from the repo. (Deploy-time VPN behaviour is verified on the host; these static checks prove the intended wiring.)
- [ ] Step 7: Document the new behaviour in README.md: the Settings tab (scrape time default 0:00, bookbub daily time default 18:00, cover size default 1x; primary-only), that bookbub fetch + owned_in_grimmory refresh + dedupe all run at the single configured daily time, the cover-size dropdown and cover hover tooltip on the BookBub Deals tab, the on-demand VPN (up only during the daily run, not at boot), and that restarting never triggers a bookbub/grimmory update (only the configured daily cron does). Update the configuration/env section and any now-stale text that describes the hardcoded 18:00 systemd timer or the always-on VPN. No secrets involved.
  - files: README.md
  - verification: grep -q 'Settings tab' README.md && grep -q 'cover-size' README.md && grep -q 'cover size' README.md && grep -q 'dedup' README.md && grep -q 'on-demand' README.md (or equivalent literals proving cover-size, dedup, settings tab, and VPN-on-demand are documented) all exit 0; and PYTHONPATH=. .venv/Scripts/python.exe scripts/_smoke.py still ends 'ALL CHECKS PASSED'.
