# Add "BookBub Deals" tab to the webapp

Task: Lets add the new scripts to the repo then modify the Amazon Wishlist Webapp
1. Lets add a new tab "BookBub Deals"
2. The tab will have a list of current deals
3. For each book, there will be - Title, Author, Deal price, Regular price, Date of Deal
4. The Title will be a link to the Amazon book which will open in the browser in a new tab
5. Keep the GUI style similar to the rest of the Webapp
6. Expired and unverified deals will not be on the list

- [ ] Step 1: Add a `current_deals(conn)` read helper to `app/deals_db.py` that returns the list of live BookBub deals for the new tab. It must select rows from the `deal` table where `deal_status = 'current'` AND `amazon_url IS NOT NULL` (so every returned row has an Amazon link to point the title at), ordered newest-first by `date DESC, id DESC`, and return dicts `{id, title, author, date, deal_price, original_price, amazon_url}` with `date` reformatted from `YYYYMMDD` to `YYYY-MM-DD` for display. This enforces requirement 6 (expired / unverified / unknown deals are excluded) at the query layer. Assumption recorded: `unknown` status is treated as unverified and therefore excluded (a book that could not be read is not presented as a live deal).
  - files: app/deals_db.py
  - verification: Create a throwaway temp deals.db, seed it via `deals_db.ensure_schema` + inserts with one row per status (current, expired, unknown, NULL/unchecked, and a current row with NULL amazon_url), call `deals_db.current_deals(conn)`, and assert it returns exactly ONE dict (the current row WITH amazon_url) with the expected fields and `date` formatted as YYYY-MM-DD. Command: a `python -c`/temp-script that prints `OK` and exits 0 when the assertion holds; failing statuses/rows must not appear.
- [ ] Step 2: Wire the new tab into the HTTP layer and UI. In `app/routes/pages.py` add a GET `/bookbub-deals` route: open `deals_db.connect(config.DEALS_DB)`, `deals_db.ensure_schema(conn)` (idempotent), fetch `deals_db.current_deals(conn)`, close, `paginate(...)` with `base_url="/bookbub-deals"`, and render a new `bookbub_deals.html` template via `_ctx({... "active": "bookbub"})`. Create `app/templates/bookbub_deals.html` extending `base.html` (matching the repo style: `<h2>`, an empty-state `<p class="empty">` when no rows, the `_pagination.html` include) with a table showing: Title (a link to `amazon_url`, `target="_blank" rel="noopener"`), Author, Deal price (`deal_price`), Regular price (`original_price`), and Date of Deal (`.ts` cell). Add the nav link in `app/templates/base.html`: `<a href="/bookbub-deals" class="{{ 'active' if active == 'bookbub' }}">BookBub Deals</a>` placed after the existing "Deals" link. Read-only and role-safe on both instances since it only reads deals.db.
  - files: app/routes/pages.py, app/templates/bookbub_deals.html, app/templates/base.html
  - verification: Use the in-repo `TestClient(app)` pattern (as in scripts/_smoke.py) pointed at a throwaway DEALS_DB (set `DEALS_DB` env to a temp db seeded via `deals_db.ensure_schema` with one current row + one expired row + one unchecked row) BEFORE importing `app.main`; `with TestClient(app) as c:` then assert `c.get('/bookbub-deals')` returns 200, contains the current title, and does NOT contain the expired/unchecked titles, and that `c.get('/')` renders a nav containing `BookBub Deals`. Exit 0 = pass.
- [ ] Step 3: Document the new tab in `README.md` — the authoritative operator manual per CLAUDE.md. Add a short section under the app's UI/pages docs describing the BookBub Deals tab: what it lists (BookBub deals currently verified live on Amazon), the five shown columns (Title→Amazon link in a new tab, Author, Deal price, Regular price, Date of Deal), that its source is `data/deals.db` (`DEALS_DB`) with `deal_status = 'current'`, and that expired/unverified/unknown deals are omitted (requirement 6). No code change.
  - files: README.md
  - verification: `grep -q "BookBub Deals" README.md` and `grep -q "deal_status" README.md` both exit 0 (the section exists), and the app still imports cleanly (`python -c "from app.main import app; print('ok')"` exits 0).
