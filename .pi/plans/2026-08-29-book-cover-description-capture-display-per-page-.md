# Book cover + description capture & display, per-page dropdown, owned-refresh, and deals mirror on BookBub tab

Task: Lets modify the webapp
1. When checking if the deal is current on Amazon, save a copy of the book cover. This book cover will be shown by the title on the Deals list page. The book cover will be stored locally
2. When checking if the deal is current on Amazon, save a copy of the book description. This book description will be shown as a tooltip when I hover over the title. The book description will be stored locally
3. Add a dropdown to change the number of titles shown per page (20, 40, 60, 80, 100) - default to 20
4. The daily amazon wishlist updater will also update the books I own from the grimmory app
5. Update the api so the secondary amazon wishlist tracker can pull all the details needed to duplicate the bookbub deals page
6. This will update with everything else at the same time the secondary amazon wishlist updates once a day

- [ ] Step 1: Add cover + description storage to the `deal` table (req 1/2 storage). In `app/deals_db.py`: add `cover TEXT` and `description TEXT` to SCHEMA_SQL, add both to the additive `_migrate()` list (no-op if present, same pattern as `hidden`), and update `current_deals()`'s SELECT list + row unpack so each returned dict also carries `cover` and `description` (NULL when never captured). Add + export `update_cover_desc(conn, row_id, cover, description)` (caller commits; the verify loop's persist hook). Add both names to `__all__`. Existing callers unaffected (defaults NULL). Assumption: cover stored as a *filename* on disk (the actual image bytes live in `data/covers/`, added next step), description stored as inline TEXT in the row.
  - files: app/deals_db.py
  - verification: `.venv/Scripts/python.exe -m py_compile app/deals_db.py` succeeds; then a gitignored data/ temp script builds a legacy `deal` table WITHOUT cover/description in a temp deals.db, runs `ensure_schema()`, asserts `PRAGMA table_info(deal)` now lists `cover` + `description`, inserts a current-status row, calls `update_cover_desc(id,'B0TEST.jpg','A blurb')` + `conn.commit()`, and asserts `current_deals(conn)` returns that row with `cover=='B0TEST.jpg'` and `description=='A blurb'`. All asserts pass = OK.
- [ ] Step 2: Capture the cover image and description during Amazon verification (req 1/2 capture), + add the covers directory config. (a) `app/config.py`: add `DEALS_COVERS_DIR = Path(os.environ.get("DEALS_COVERS_DIR", DATA_DIR / "covers"))`. (b) `app/amazon_price.py`: add a PURE selectolax parser `extract_page_meta_html(html) -> {"cover_url", "description"}` reading the Amazon product image (`#landingImage` / `#imgBlkFront` → `src` / `data-old-hires`) and the description block (`#bookDescription_feature_div` / `#productDescription` innerText, whitespace-collapsed) — best-effort, never raises, returns empty strings when absent (selectors co-located for easy adjustment, mirroring the BookBub/price conventions). Add `download_cover(url, dest, fetcher=None) -> Path|None` that lazily imports httpx (or uses the injected `fetcher` for tests), writes the cover bytes to `dest` (mkdir parents), derives the extension from the URL path, and never raises (logs + returns None on failure). (c) `scripts/verify_deals.py`: extend the per-book `read` closure so that after `amazon_price.read_page` for each deal it grabs `page.content()`, runs `extract_page_meta_html`, downloads the cover into `DEALS_COVERS_DIR` (filename `<asin>.<ext>`), and persists `cover` + `description` via `deals_db.update_cover_desc(...)` + `conn.commit()` — best-effort and logged, never aborting the run (a cover failure must not fail an otherwise-fine verification, consistent with the 'best-effort, never abort' convention). Return cover/desc from the read closure so they reach the persist call. Assumption recorded: extraction is HTML-based via selectolax (offline-testable), the Playwright layer only feeds `page.content()`.
  - files: app/config.py, app/amazon_price.py, scripts/verify_deals.py
  - verification: `.venv/Scripts/python.exe -m py_compile app/config.py app/amazon_price.py scripts/verify_deals.py` succeeds; a gitignored data/ temp script (1) calls `extract_page_meta_html` on a canned HTML string containing `<img id='landingImage' src='https://m.media-amazon.com/images/I/51ABC.jpg'>` and `<div id='bookDescription_feature_div'>A long  blurb </div>` and asserts it returns that cover_url and a collapsed description; (2) calls `download_cover` with a stub `fetcher=lambda url: b'FAKEBYTES'` into a temp dir and asserts the file exists with content `b'FAKEBYTES'` and the returned Path names the asin with a `.jpg`/`.jpeg` ext; (3) asserts `config.DEALS_COVERS_DIR` resolves under `data/covers`. All pass = OK.
- [ ] Step 3: Daily owned-in-grimmory refresh (req 4). In `app/deals_db.py`: add + export `refresh_owned(conn, grimmory_path) -> int` that reads every row's `(bookbub_url, title, author)` and recomputes `owned_in_grimmory` for ALL rows against `grimmory.db` (reusing the existing normalise/owned_lookup matching; missing grimmory.db sets the flag to NULL), returning the number of rows updated (caller commits). In `scripts/bookbub_daily.py`: in the real-run path, after `store_deals`, call `deals_db.refresh_owned(conn, GRIMMORY_DB)` (open the deals DB, ensure_schema, refresh, commit) wrapped so a missing/empty grimmory.db leaves flags NULL and never aborts the run. Assumption (recorded): grimmory.db is refreshed out-of-band on the host (build_grimmory_db.py) as new books are added — the daily updater runs inside the wlvpn netns where the grimmory server is not reachable, so ownership is re-applied from the local grimmory.db file, not by calling the grimmory API.
  - files: app/deals_db.py, scripts/bookbub_daily.py
  - verification: `.venv/Scripts/python.exe -m py_compile app/deals_db.py scripts/bookbub_daily.py` succeeds; a gitignored data/ temp script seeds a temp deals.db where a row ('https://www.bookbub.com/books/a','The Testaments','M Atwood') has owned_in_grimmory=0 and a row ('.../b','Other','X Author') has owned_in_grimmory=1, creates a temp grimmory.db with a matching 'The Testaments'/'M Atwood' book, calls refresh_owned, and asserts row a→1, row b→0 (updated count correct); then deletes grimmory.db and re-refreshes → both flags become NULL. Also runs `scripts/bookbub_daily.py --check --db <temp>` and asserts exit 0 with the recheckable count. All pass = OK.
- [ ] Step 4: BookBub Deals page UI (req 1/2 display + req 3). (a) `app/config.py`: add `BOOKBUB_PER_PAGE_OPTIONS = [20, 40, 60, 80, 100]` and `BOOKBUB_PER_PAGE_DEFAULT = 20`. (b) `app/routes/pages.py`: in `bookbub_deals_page`, change `per_page: int = Query(BOOKBUB_PER_PAGE_DEFAULT)` and add `_bookbub_per_page(value)` that snaps an incoming per_page to the nearest allowed option; add `per_page` to the `extra_query` so pagination links and the dropdown preserve it. Update the route docstring (it currently claims deals.db is 'never synced' — now it is synced via the mirror; fix the stale sentence). (c) `app/routes/pages.py` (or `api.py`): add `GET /covers/{name}` returning a `FileResponse` from `config.DEALS_COVERS_DIR`, guarding path traversal (`Path(name).name` and a resolver check that the final path stays inside the dir; 404 otherwise). (d) `app/templates/bookbub_deals.html`: render a cover `<img class='deal-cover'>` (src `/covers/{{ r.cover }}`, alt title) when `r.cover` is set, add the `title="{{ r.description }}"` hover tooltip on the title link (Jinja auto-escapes), and add a per-page `<select>` (options 20/40/60/80/100, default 20, onchange navigates to `/bookbub-deals` preserving sort/dir/show_hidden + `per_page=<chosen>`).
  - files: app/config.py, app/routes/pages.py, app/templates/bookbub_deals.html
  - verification: `.venv/Scripts/python.exe -m py_compile app/config.py app/routes/pages.py` succeeds. A gitignored data/ temp TestClient script sets `DEALS_DB` to a temp deals.db seeded with 25 current-status rows (some with cover/description), starts `TestClient(app)` (env DEALS_DB + DEALS_COVERS_DIR to temp): (1) GET /bookbub-deals defaults to page size 20 (asserts 20 row `tr`s and that the per-page `<select>` lists options 20..100), and `?per_page=40` renders 40; (2) a row with cover renders `<img class='deal-cover' src='/covers/...'>` and the title link carries the `title=` description; (3) writing a file to the temp covers dir then GET /covers/<file> returns 200 with those bytes, and GET /covers/../../etc/passwd returns 404. Then `PYTHONPATH=. .venv/Scripts/python.exe scripts/_smoke.py` still ends 'ALL CHECKS PASSED'. All pass = OK.
- [ ] Step 5: Deals DB + covers sync export/apply API (req 5). In `app/sync.py`: add `export_deals() -> dict` that reads the whole `deal` table (all columns: id,date,title,author,deal_price,original_price,bookbub_url,amazon_url,no_amazon_link,owned_in_grimmory,audited_at,deal_status,current_price,verified_at,hidden,cover,description) from `config.DEALS_DB` (via `deals_db.connect`), plus a `covers` dict `{cover_filename: base64_bytes}` for every cover file present in `DEALS_COVERS_DIR` (missing/unreadable files skipped), returning `{"format": SYNC_FORMAT, "source_now": ..., "deals": [...], "covers": {...}}`. Add `apply_deals(payload, covers_dir) -> dict` that replaces the local `deal` rows wholesale (DELETE + re-INSERT with the primary's explicit ids, inside `deals_db.connect` BEGIN/COMMIT, ROLLBACK on error) and writes every cover file into `covers_dir` (mkdir parents, atomic tmp+os.replace, base64 decode; failures logged not fatal). Both get a `_check_deals_format`. In `app/routes/sync.py`: add `GET /api/sync/deals` returning `sync.export_deals()`. Assumption recorded: the deals table is small (~400 rows) so a whole-table wholesale replace on each sync (no watermark, no FK coupling to wishlist.db) is correct for a read-only mirror; covers travel as base64 in the same payload (acceptable for a once-a-day LAN sync).
  - files: app/sync.py, app/routes/sync.py
  - verification: `.venv/Scripts/python.exe -m py_compile app/sync.py app/routes/sync.py` succeeds. A gitignored data/ temp script seeds a source deals.db (2 rows incl cover+description+hidden+owned) + a covers dir with one file, calls `sync.export_deals()` and asserts the payload carries both rows and the cover base64; then `sync.apply_deals(payload, dest_covers_dir)` into a fresh deals.db and separately into a fresh covers dir, and asserts the dest deal rows match the source on all columns and the cover file is written with matching bytes; a second `apply_deals` of the same payload leaves the dest unchanged (idempotent). Also, a `TestClient(app)` run returns 200 for `/api/sync/deals` with `format == sync.SYNC_FORMAT`. All pass = OK.
- [ ] Step 6: Wire the deals sync into the once-a-day mirror pull (req 6). In `app/sync_client.py`: within `_pull()`, after the catalog (and before/after snapshots is fine — the deals DB has no FK coupling to wishlist.db), fetch `GET /api/sync/deals` and `sync.apply_deals(payload, config.DEALS_COVERS_DIR)`, returning a `deals` count in the summary. Add a small `_pull_deals(client)` helper so the step is independently testable. Make the deals pull GRACEFUL when the primary predates it: a 404 from `/api/sync/deals` (or a missing key) logs a warning and does not abort the wishlist sync, so mixed-version peers keep working. Because `_pull` already runs on the secondary's daily APScheduler cron AND at startup (main.py), requirement 6 ('updates with everything else once a day') is satisfied with no new timer. Update the mirror-invariants note in `app/sync.py`'s module docstring (it says only wishlist snapshots sync; add one sentence that deals.db + covers now mirror via `/api/sync/deals` on the same daily pull).
  - files: app/sync_client.py, app/sync.py
  - verification: `.venv/Scripts/python.exe -m py_compile app/sync_client.py app/sync.py` succeeds. A gitignored data/ temp script seeds a source deals.db + covers dir, computes the `export_deals()` payload, builds an `httpx.Client` with `httpx.MockTransport` that answers `GET /api/sync/deals` (any path) with that payload, opens a secondary deals.db + covers dir via config monkeypatching, calls `sync_client._pull_deals(client)`, and asserts the secondary's deals.db rows mirror the source and the cover file was written. It also asserts a 404-returning transport produces a warning but does not raise. Then `PYTHONPATH=. .venv/Scripts/python.exe scripts/_smoke.py` still ends 'ALL CHECKS PASSED' (the existing wishlist sync path is untouched). All pass = OK.
- [ ] Step 7: README documentation (final step). Update README.md for all six requirements: (a) the BookBub Deals tab section — cover image shown by the title (served from `/covers/...`, stored locally under `data/covers/`), description shown as a hover tooltip (`title` attr) on the title, and the per-page dropdown (20/40/60/80/100, default 20) with `per_page` preserved across sort/pagination; (b) the 'Verifying deals are still live' section — note that each verification now also captures the book cover (downloaded to `data/covers/`) and the description (stored in `data/deals.db`); (c) the 'Daily BookBub updater' section — note ownership (`owned_in_grimmory`) is refreshed against `data/grimmory.db` every run; (d) the two-instance mirror section — document that `deals.db` + covers now mirror to the secondary via `GET /api/sync/deals` in the same once-a-day pull, so a secondary can duplicate the BookBub Deals page, and correct/adjust any now-stale text claiming deals.db is never synced; (e) the env var table — add `DEALS_COVERS_DIR`, `BOOKBUB_PER_PAGE_OPTIONS`, `BOOKBUB_PER_PAGE_DEFAULT`. No secrets are involved (covers/descriptions are not credentials).
  - files: README.md
  - verification: From the repo root: `grep -q 'DEALS_COVERS_DIR' README.md && grep -q '/api/sync/deals' README.md && grep -q 'BOOKBUB_PER_PAGE_DEFAULT' README.md && grep -q 'deal-cover' README.md && grep -q 'refresh' README.md` all exit 0 (the last is a sanity that the owned-refresh is documented; if 'refresh' is too generic, the builder may use any one literal string proving ownership refresh is documented, e.g. 'owned_in_grimmory'). Then `.venv/Scripts/python.exe -c "from app.main import app; print('ok')"` from the repo root prints ok. All pass = OK.
