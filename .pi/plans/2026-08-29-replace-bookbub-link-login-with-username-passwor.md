# Replace BookBub link login with username/password credentials

Task: Lets modify the webapp
1. I have a bookbub account - Username - REDACTED-BOOKBUB-USERNAME , Password - REDACTED-BOOKBUB-PASSWORD
2. Modify the app to this instead of the unset link

- [ ] Step 1: Add BookBub credential plumbing to `app/config.py`, mirroring the existing GRIMMORY_USERNAME/GRIMMORY_PASSWORD secret pattern (env-read, NO committed default). Add `BOOKBUB_USERNAME = os.environ.get("BOOKBUB_USERNAME", "").strip()` and `BOOKBUB_PASSWORD = os.environ.get("BOOKBUB_PASSWORD", "")` right beside the existing `BOOKBUB_LOGIN_LINK` line. The values are injected at run time (on the host via `/etc/default/amazon-wishlist`), never committed; an empty value means "not configured". Recorded assumption and hard rule: the real credentials are NEVER written into any tracked file — the repo only holds the env plumbing.
  - files: app/config.py
  - verification: Offline subprocess checks: `BOOKBUB_USERNAME=user@example.com BOOKBUB_PASSWORD=redacted .venv/Scripts/python.exe -c "from app import config; assert config.BOOKBUB_USERNAME=='user@example.com' and config.BOOKBUB_PASSWORD=='redacted'; print('ok')"` (env-read works) and, without env, `assert config.BOOKBUB_USERNAME=='' and config.BOOKBUB_PASSWORD==''` (empty default). Then `grep -n 'BOOKBUB_USERNAME\|BOOKBUB_PASSWORD' app/config.py` shows they read from `os.environ.get(...)` with an empty/`""` default. All exit 0 = pass.
- [ ] Step 2: Implement credential-based BookBub login in `app/bookbub.py` and wire it into the Playwright fetch path as the primary login, keeping the existing outbound-link path as a backward-compatible fallback. (a) Add module constants: `LOGIN_URL = "https://www.bookbub.com/login"` and login-field selectors (`LOGIN_EMAIL_SEL`, `LOGIN_PASSWORD_SEL`, `LOGIN_SUBMIT_SEL`) — a single, documented place to adjust if BookBub's markup differs (the repo's "DOM drift → update selectors in one place" convention). (b) Add `_login(page, username, password)` that navigates to `LOGIN_URL`, clears the Cloudflare challenge (`_wait_past_challenge`), fills email/password into the selectors, submits, and clears the challenge again (best-effort; on a page that already reads non-challenged it returns immediately). (c) Update `fetch_daily_deals_playwright`, `fetch_daily_deals_many`, and the public `fetch_daily_deals` so that when credentials are supplied (explicit arg or `BOOKBUB_USERNAME`/`BOOKBUB_PASSWORD` env) they log in via `_login` instead of `page.goto(link)`; the `link=` argument is retained and used only when no credentials are configured (so `scripts/build_bookbub_deals.py` and the probe keep working). Raise `BookbubError` with a clear message when neither credentials nor a link is available. Assumption recorded: BookBub's login page is at `/login` with a standard email/password form; these selectors are validated live on the host once (single chokepoint to adjust).
  - files: app/bookbub.py
  - verification: `.venv/Scripts/python.exe -m py_compile app/bookbub.py`; plus an offline temp test (gitignored data/) that (A) builds a fake Playwright page recording `goto/fill/click` calls and calling `bookbub._login(fake, 'u@x.com', 'pw')` — asserts it navigated to `bookbub.LOGIN_URL`, filled the email/password selectors with those values, and clicked the submit selector; and (B) asserts `fetch_daily_deals(date)` with no creds and no link raises `BookbubError` whose message mentions login/credentials (config env cleared before import in the subprocess). Command exits 0 = pass. (The real login against bookbub.com is validated on the host after deploy using the operator's real creds, not in this offline gate.)
- [ ] Step 3: Point the daily updater `scripts/bookbub_daily.py` at the new credential login instead of the outbound link. Remove the `--link`/BOOKBUB_LOGIN_LINK dependency and pass `username`/`password` (from `BOOKBUB_USERNAME`/`BOOKBUB_PASSWORD`, imported from config) to `bookbub.fetch_daily_deals`. Real run exits 2 with a clear message when neither credential is configured (before touching DB/browser/network), matching the previous no-link behaviour; a fetch failure still exits 1 and the re-verify step still runs. The `--check` dry run reports the DATE and a credential status — `"credentials: set"` or `"no BOOKBUB_USERNAME / BOOKBUB_PASSWORD configured"` — plus the recheckable count, and NEVER prints the actual values. Update the module docstring accordingly.
  - files: scripts/bookbub_daily.py
  - verification: `.venv/Scripts/python.exe -m py_compile scripts/bookbub_daily.py`; then (no network, BOOKBUB_USERNAME/PASSWORD cleared from the env) `.venv/Scripts/python.exe scripts/bookbub_daily.py --check --db <temp seeded deals.db> --netns wlvpn` exits 0 and prints 'no BOOKBUB_USERNAME / BOOKBUB_PASSWORD configured'; and the default run (no --check, no creds) exits 2 with a clear message and the temp DB row count is unchanged. Also, with `BOOKBUB_USERNAME=u@x.com BOOKBUB_PASSWORD=redacted`, `--check` prints 'credentials: set'. Command (a small gitignored data/ script invoking the subprocesses) exits 0 = pass.
- [ ] Step 4: Update `README.md`: replace the `BOOKBUB_LOGIN_LINK` prerequisite with the credential mechanism in the "Daily BookBub updater" and BookBub sections — `BOOKBUB_USERNAME` / `BOOKBUB_PASSWORD` go in `/etc/default/amazon-wishlist` on the host; they are credentials read from the environment and **never committed to the repo** (same rule as GRIMMORY_USERNAME/PASSWORD); rotate the account password if ever exposed. Keep a one-line note that an optional outbound-link login still works for ad-hoc/probe use via `--link`. The verification greps do NOT need the real values — only the env-var names.
  - files: README.md
  - verification: `grep -q 'BOOKBUB_USERNAME' README.md` and `grep -q 'BOOKBUB_PASSWORD' README.md` exit 0, and the committed repo contains NO non-empty BookBub credential: `grep -ERn 'BOOKBUB_PASSWORD *=\s*[^"\"]' app scripts README.md` matches nothing (exit 1 for grep) meaning no literal default is committed. `.venv/Scripts/python.exe -c 'from app.main import app; print("ok")'` from the repo root prints ok / exit 0.
