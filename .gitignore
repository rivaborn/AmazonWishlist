__pycache__/
*.pyc
.venv/
venv/
data/
# Explicitly ignore the one-off Grimmory book catalog build (already covered by
# the data/ rule; named here so it is unambiguous it never lands in git).
data/grimmory.db
*.db
*.log
.env
.idea/
.vscode/
scripts/_bootstrap_ssh.py
